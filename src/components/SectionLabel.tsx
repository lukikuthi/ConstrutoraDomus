const SectionLabel = ({ children }: { children: string }) => (
  <span className="text-xs font-body font-medium tracking-[0.3em] uppercase text-accent mb-4 block">
    {children}
  </span>
);

export default SectionLabel;
